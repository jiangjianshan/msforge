<div align="center">
  <h1>✨🚀 msforge 🚀✨</h1>
</div>

## Выбор языка
[English](./README.md) | [简体中文](./README.zh-CN.md) | [Español](./README.es.md) | [日本語](./README.ja.md)  
[한국어](./README.ko.md) | **Русский** | [Português](./README.pt-BR.md)

## Обзор проекта
`msforge` — это фреймворк для сборки, разработанный специально для среды Windows MSVC. Его основная ценность заключается в преобразовании рутинных и подверженных ошибкам ручных операций сборки в надежные автоматизированные процессы. Это позволяет разработчикам сосредоточиться на оптимизации и совершенствовании рецептов сборки, а не погружаться в сложности низкоуровневых инструментов.

## Ключевые особенности
- **Поддержка нескольких систем сборки**: Нативная поддержка основных систем сборки, таких как CMake, Meson, Autotools, с автоматическим обнаружением и настройкой соответствующей среды компиляции.
- **Минимальные зависимости среды**: Основан на Git for Windows и небольшом наборе необходимых компонентов autotools, что позволяет работать с проектами Autotools без необходимости установки полной Cygwin/MSYS2.
- **Интеллектуальное управление зависимостями**: Поддержка сложного разрешения зависимостей и топологической сортировки, обеспечивающая правильный порядок сборки и полную цепочку зависимостей.
- **Удобство для пользователя**: Интеграция с библиотекой [Rich](https://github.com/Textualize/rich) обеспечивает красочный вывод в терминале, отображая прогресс сборки и статусную информацию в реальном времени.
- **Надежный фреймворк сборки**: Предоставляет большое количество проверенных скриптов сборки для библиотек (в каталоге `ports`), в которых уже решены многочисленные проблемы компиляции под MSVC для различных открытых библиотек.
- **Эффективный процесс разработки**: Разработчику достаточно объявить метаинформацию о библиотеке и сосредоточиться на конфигурации сборки, в то время как сложные низкоуровневые операции, такие как загрузка, обработка зависимостей, инкрементная сборка и т.д., прозрачно обрабатываются фреймворком.
- **Полное управление жизненным циклом**: Обеспечивает полный цикл управления от получения исходного кода, сборки и установки до очистки и удаления, поддерживает гибкую настройку путей установки.

`msforge` продолжает развиваться и совершенствоваться, и мы приветствуем ваше участие и вклад! Если нужная вам библиотека еще не поддерживается, вы можете [создать issue](https://github.com/jiangjianshan/msforge/issues) или добавить ее самостоятельно, следуя [руководству по внесению вклада](#руководство-по-внесению-вклада).

## Быстрый старт
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/jiangjianshan/msforge.git
cd msforge

# 2. Просмотрите все доступные команды и опции
mpt --help

# 3. Соберите и установите все поддерживаемые библиотеки одной командой (по умолчанию собирается для архитектуры x64)
mpt
```
После завершения установки собранные библиотеки можно будет использовать в ваших проектах. Если вы не хотите использовать путь установки по умолчанию, вы можете использовать опцию `--<имя_библиотеки>-prefix` для указания пользовательского пути установки для каждой библиотеки.

## Основные способы использования

`msforge` предоставляет простой и единообразный интерфейс командной строки. Указанные ниже имена библиотек представляют лишь небольшую часть доступных в `ports`. Вся метаинформация и скрипты сборки для предоставленных библиотек находятся в каталоге `ports`.

**Установка библиотек:**
```bash
# 1. Установите библиотеки (x64 — архитектура по умолчанию)
mpt gettext gmp gsl glib fftw libxml2 llvm-project mpc mpfr OpenBLAS ncurses readline VTK

# 2. Установите библиотеки для архитектуры x86
mpt --arch x86 gettext gmp gsl glib fftw libxml2 llvm-project mpc mpfr OpenBLAS ncurses readline VTK
```

**Удаление библиотек:**
```bash
# Удалите все библиотеки, одну или несколько библиотек
mpt --uninstall
mpt --uninstall OpenCV
mpt --uninstall gettext gmp gsl glib fftw libxml2 llvm-project mpc mpfr OpenBLAS ncurses readline VTK
```

**Просмотр библиотек:**
```bash
# 1. Просмотрите статус установки всех библиотек, одной или нескольких библиотек
mpt --list
mpt --list OpenCV
mpt --list gettext gmp gsl glib fftw libxml2 llvm-project mpc mpfr OpenBLAS ncurses readline VTK

# 2. Отобразите красиво отрендеренное дерево зависимостей для всех библиотек, одной или нескольких библиотек
mpt --dependency
mpt --dependency OpenCV
mpt --dependency gettext gmp gsl glib fftw libxml2 llvm-project mpc mpfr OpenBLAS ncurses readline VTK
```

**Добавление/удаление библиотек:**
```bash
# Добавьте или удалите одну или несколько библиотек
mpt --add <имя_новой_библиотеки>
mpt --add <имя_новой_библиотеки1> <имя_новой_библиотеки2>
mpt --remove <имя_существующей_библиотеки>
mpt --remove <имя_существующей_библиотеки1> <имя_существующей_библиотеки2>
```

**Только загрузка/клонирование:**
```bash
# Загрузите архивы и распакуйте их, или клонируйте исходный код (только для Git-репозиториев) для всех библиотек, одной или нескольких библиотек
mpt --fetch
mpt --fetch OpenCV
mpt --fetch gettext gmp gsl glib fftw libxml2 llvm-project mpc mpfr OpenBLAS ncurses readline VTK
```

**Очистка кэша:**
```bash
# Интерактивная очистка файлов журналов, архивов и каталогов исходного кода для всех библиотек, одной или нескольких библиотек
mpt --clean
mpt --clean OpenCV
mpt --clean gettext gmp gsl glib fftw libxml2 llvm-project mpc mpfr OpenBLAS ncurses readline VTK
```
Запустите `mpt --help`, чтобы просмотреть полный список команд и примеров.

## Руководство по внесению вклада

`msforge` уже успешно собрал большое количество различных открытых библиотек и продолжает расширяться. Полный список поддерживаемых библиотек можно посмотреть с помощью команды `mpt --list`. Мы искренне благодарны за любой ваш вклад.

**Вы можете принять участие следующим образом:**
*   [Создать issue](https://github.com/jiangjianshan/msforge/issues): Сообщить об ошибке или предложить новую функцию.
*   [Добавить новую библиотеку](#добавление-новой-библиотеки): Добавьте новую библиотеку или улучшите существующую, следуя приведенной ниже процедуре.

### Добавление новой библиотеки

1.  **Создайте шаблон для библиотеки:**
```bash
mpt --add <имя_библиотеки>
```
Сгенерированный файл конфигурации библиотеки `config.yaml` можно вручную скорректировать.

2.  **Применение патчей (опционально)**: Если требуются специфичные для Windows исправления, создайте файл(ы) `.diff`.
3.  **Написание скрипта сборки**: Создайте `build.bat` или `build.sh` в каталоге `ports/<имя_библиотеки>`. Можно ориентироваться на существующие примеры.
4.  **Тестирование и отправка:**
```bash
mpt <имя_библиотеки> # Собрать и протестировать
```
После успешного тестирования отправьте Pull Request, содержащий каталог `ports/<имя_библиотеки>`.

Более подробную информацию можно найти в конфигурациях существующих библиотек в каталоге `ports`.

## Ресурсы

*   **Исходный код и конфигурации библиотек:** https://github.com/jiangjianshan/msforge
*   **Issues и обсуждения:** https://github.com/jiangjianshan/msforge/issues